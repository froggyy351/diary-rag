# diary-rag

自分の日記・メモに自然言語で質問できる **RAGチャットアプリ**を Python で自作しながら、
実務で通用する実装力をつける学習プロジェクト。

**このファイルは公開されます。** 所属・時期・キャリアにかかわる前提はローカルの
非公開設定側にあり、ここには書きません。こちらに書くのは
**何を作るか・どう進めるか・技術判断**だけです。

（最終更新: 2026-09-05 / AWS 中止・期限確定を受けて全面改訂）

## ゴール

- **2027/2末までに、ブラウザのチャット画面から自分の日記に質問できる状態にする**
- 「知っている」ではなく「**手が動く**」を基準にする。読んで分かったものは作れたことにしない
- 完成物はポートフォリオとして人に見せられる状態にする

### 進み方の規律

- **週1コミットが最低ライン。** 2026/8/11〜8/25 に2週間止まった前例がある。
  Claude は進捗確認時にコミット状況を見て、途切れていたら指摘する
- マイルストーンは「トピックを勉強した」ではなく **「何をコミットしたか」**で判定する
- 想定は週11h。ただし**週の半分しか取れなくても計画は壊れない**余裕がある（下記ロードマップ）

## 最重要方針: コードを代わりに書かないこと

これは**学習**プロジェクトです。Claude がコードを全部生成したら目的が崩壊します。

- ユーザーが自分で書く。Claude は**設計の相談・ヒント・書いたコードのレビュー**に徹する
- 詰まったときは答えを渡さずヒントを渡す
- 型ヒントを最初から書く習慣をつけさせる。Java 経験者に馴染みやすく、実務でも必須

### 実行はユーザー自身が行う

コードだけでなく、**git 操作・ファイル作成・コマンド実行の全般**に適用される。

- Claude はツール（Bash / Write / Edit）で作業を代行しない。コマンドや設定ファイルの
  雛形を**テキストとして提示し、説明する**ところまでが役割
- 読み取り専用の確認（`git status`、`ls`、ファイルを読む）は Claude がやってよい
- 例外はユーザーから明示的に「代わりにやって」と依頼されたときだけ。
  **過去に一度許可したことは、以後の許可にはならない**

## 現在地（2026-09-05）

- **リポジトリはまだ空**（コミット1本、`.gitignore` のみ）。`uv init` が次の一手
- Python 文法と FastAPI の CRUD は習得済み。まだ不安な所もある
  （練習用: https://github.com/froggyy351/python-playground ）
- **未経験**: DB層（SQLAlchemy / PostgreSQL / Alembic）、テスト（pytest / TestClient）、
  プロジェクト構成（`pyproject.toml` / APIRouter 分割）、RAG 一式、Docker、フロント（React）
- スキルの詳細は非公開設定側を参照

## 技術選定（決定済み・以後迷わない）

| 項目 | 選択 | 理由 |
|---|---|---|
| パッケージ管理 | uv | venv と pip の仕事を1つで賄える |
| DB | PostgreSQL（docker compose） | 実務で最も出会う。ベクトル拡張も同じ DB に載る |
| ORM / マイグレーション | SQLAlchemy 2.0 + Alembic | Spring Data JPA + Flyway の対比で理解する |
| ベクトルストア | **pgvector** | PostgreSQL を既に立てるので部品が増えない。永続化を自分で書かずに済む |
| LLM / 埋め込み | **Google Gemini（無料枠）** | 埋め込みと生成の**両方が1つの無料枠**で賄える。カード登録も不要 |
| フレームワーク | FastAPI + LangChain | |
| フロント | Vite + React + TypeScript + TailwindCSS | |

- **FAISS** は「ライブラリ単体のベクトル検索」を理解するため10月に一度触るだけ。本採用しない
- **Gemini を選んだ理由（補足）**: 埋め込み API を持たない提供元だと RAG が完結しない。
  無料枠の範囲で完結する見込みで、超えても月数百円規模。LangChain 経由で書いておけば、
  将来モデルを差し替えるときは指定の行を変えるだけで済む
- **注意: 無料枠は入力が学習に使われる場合がある。業務の文書は絶対に入れない**

## ロードマップ

**動くものを最短で1本通してから改良する。** 完了条件はコミットで判定する。

| 時期 | やること | 完了条件 |
|---|---|---|
| **9月** | `uv init` でプロジェクト化（`pyproject.toml` + APIRouter 分割）→ docker compose で PostgreSQL → SQLAlchemy で永続化 → Alembic → pytest + TestClient | 永続化された CRUD API が動き、pytest が通り、マイグレーション履歴がある |
| **10月** | 読み込み → チャンク分割 → 埋め込み → pgvector 格納 → CLI で「似ている日記」が引ける → LLM に渡して回答 → `POST /chat` として API 化 → 会話履歴を DB 保存 | 「先月何に悩んでた？」に、根拠の日記を引用しながら答えが返る |
| **11月** | `eval/questions.md` を自動で流すスクリプト → チャンクサイズ3パターン（例 300/800/1500字）を比較 → 検索ログを残す | 回答品質を**数字で**測れる。改善の前後を比較できる |
| **12月** | LangGraph で「日記を検索して答える / 検索せず答える」を分岐するグラフ | 分岐が動く。※年末年始で2週消える前提の軽い月 |
| **2027/1** | React + Tailwind の最小チャットUI（下記スコープ厳守） | ブラウザから質問して答えが返る |
| **2027/2** | Dockerfile を書く → `docker compose up` 一発で api + front + DB が立つ → README にアーキテクチャ図 | 他人がクローンして動かせる |

### 9月の内訳（W1〜W4）

| 週 | 成果物 |
|---|---|
| W1 (9/1〜) | ① `uv init` でプロジェクト化し、playground の todo API を移植して起動する<br>② テストコーパスの**設計メモ**を書く（生成は W4 でよい） |
| W2 | docker compose で PostgreSQL を起動し、SQLAlchemy 2.0 で `dict` を卒業。**ここが最初の Docker 体験** |
| W3 | Alembic でマイグレーション。pytest + TestClient で CRUD のテスト（JUnit + MockMvc の対比） |
| W4 | 設計メモをもとに**テストコーパスを生成**（`data/sample_diary/` に100〜200件）＋ `eval/questions.md` を同時に作る |

### 優先度の2階層

配分を変えるときは**月をまたいで組み替えず、下の枠に落とす**。計画全体を作り直さずに済む。

- **必達コア**: 上の表の6ヶ月ぶん。これが「手が動く」の中身
- **余裕があれば**: Dify のソース読解（読む箇所を3つに絞る = ワークフロー実行部 /
  取り込みの非同期タスク / ベクトル検索の抽象化レイヤ）/ Celery + Redis で取り込みを
  非同期化 / グラフRAG の座学 / ハイブリッド検索 / UI のストリーミング表示（SSE）

**時間には余裕がある。** 必達コアを積み上げても想定時間の半分程度。詰め込むより
**各月の完了条件を確実に満たす**ことを優先する。余った時間は上の「余裕があれば」に回す。

### フロントエンドのスコープ（これ以上広げない）

- **作る**: 1ページのみ。メッセージ一覧 + 入力欄 + 送信ボタン + ローディング表示。
  `POST /chat` を `fetch` で叩いて並べるだけ
- **作らない**: ルーティング、状態管理ライブラリ、認証、ストリーミング、ダークモード、UIライブラリ
- **学びどころ**: CORS（FastAPI の `CORSMiddleware`）、非同期処理中の UI 状態、API の型合わせ。
  **フロントとバックが完全に別プロセスになる**感覚を掴むのが目的

## データソース

RAG に流すデータは3系統ある。**役割が違うので混ぜない。**

| ソース | 役割 | git |
|---|---|---|
| **合成コーパス** `data/sample_diary/` | **評価用**。正解が既知 | **コミットする** |
| **学習日記**（1日1件・2026/9/4〜） | **実運用インプット**。日々増える | しない |
| 過去の実日記（ローカル・数件） | 最終検証 | しない |

- **取り込み元のパスをハードコードしない。** 日記ディレクトリは設定値（env / settings）で
  渡し、デフォルトをリポジトリ内の `data/sample_diary/` にする。
  そうしないと他人の手元で動かない＝ポートフォリオとして弱い
- 学習日記は10月の実装開始時点で30〜50件たまる見込み。**実データで壊れないかの確認に使える**が、
  正解表がないので評価には使えない

## コーパス方針

`data/sample_diary/` は**合成テストデータを自作する**（実データの蓄積を待たない）。

### なぜ合成なのか

自分で作るということは、**どのファイルに何が書いてあるかを自分が知っている**ということ。
つまり**正解が既知**になる。RAG で一番難しいのは「回答が正しいかをどう判定するか」で、
実データだと目視しかできない。合成なら **質問 → 根拠ファイルの対応表（ground truth）**が
最初から手元にある。**これがそのまま11月の評価セットになる。**

### 仕込む性質（この設計こそが RAG の理解）

| 性質 | 何をテストできるか |
|---|---|
| 量: 100〜200ファイル / 1件200〜600字 | チャンク分割の挙動は少量データでは観察できない |
| 時間軸: ファイル名と本文に日付 | 「先月」への回答。ベクトル検索は日付を理解しない＝**メタデータフィルタが要る**と体で分かる |
| **継続する話題**: 同じ悩みが複数日にまたがって進展する | **本命**。散らばった複数チャンクを集めないと答えられない |
| 紛らわしい類似トピック（似ているが別の話） | 意味的に近い別物を誤って拾わないか |
| 1箇所にしかない固有名詞 | ピンポイント検索。全文検索が強くベクトルが弱い＝**ハイブリッド検索が要る理由** |
| 答えを書いていない穴 | **ハルシネーション検出**。「書かれていません」と言えるか。実務で最重要 |
| 文体のノイズ（走り書き・誤字・略語） | LLM 生成データは綺麗すぎる。実データで壊れないための保険 |
| 長さのばらつき（3行〜2000字） | チャンク境界の挙動 |

**注記**: 「時間軸」と「継続する話題」は学習日記が天然に持っている。合成側は残りの性質、
とくに**罠系（類似トピック・1箇所だけの固有名詞・穴）**に集中させると W4 が軽くなる。

### 作り方

1. 設計メモを先に書く（登場人物・進行する話題3〜4本・時期・仕込む罠）
2. それをもとに LLM に生成させる。**まとめて200件は質が落ちるので月単位で小分けにする**
3. 同時に `eval/questions.md`（質問 → 期待する答え → 根拠ファイル）を作る。
   **生成直後が一番正解を覚えているので後回しにしない**
4. 合成データなのでプライバシー問題がなく、**他人が再現できる**。実データにない強み

## 学習日記

学習を伴うセッションでは、区切りで学習日記を書く。書き方・保存先は `study-diary` スキルに従う。

- 「日記」「学習記録」「今日やったこと」と言われたら書く
- 何かを理解した流れが一区切りついたら、Claude から「日記に書いておく？」と提案する
- **この日記自体がこのアプリの入力データになる**（上記「データソース」）。
  だから**日記に業務固有の固有名詞を書かない**

## やらないと決めたこと(ゆくゆくはやりたい)

| 項目 | 理由 |
|---|---|
| **AWS 全般**（資格 / デプロイ / S3） | **2026-09-05 に中止。** Python への選択と集中。 |
| Terraform / Kubernetes | 同上。学習コスト対効果が悪い |
| Elasticsearch | 現場で学ぶ |

## その他の約束事

- **説明の仕方・分量・言葉づかいのルールは非公開設定側に集約**。ここでは繰り返さない
- Java（bookblog 等）は凍結中。Python に全振りする方針で合意済み
- 進捗は GitHub（froggyy351）にコミットしてポートフォリオ化する
- 文法や FastAPI の単発の練習は python-playground、**本番はこのリポジトリ**
